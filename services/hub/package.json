{
  "name": "@shippie/hub",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "bun test",
    "build": "echo 'built by Dockerfile'",
    "clean": "rm -rf .turbo *.tsbuildinfo .shippie-hub-state"
  },
  "dependencies": {
    "@shippie/core": "workspace:*",
    "bonjour-service": "^1.2.1"
  },
  "devDependencies": {
    "@shippie/app-package-builder": "workspace:*",
    "@shippie/app-package-contract": "workspace:*",
    "@types/bun": "^1.3.12",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
