{
  "name": "@shippie/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.dev.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint placeholder'",
    "test": "bun test",
    "build": "echo 'built by wrangler or bundle on deploy'",
    "clean": "rm -rf .turbo *.tsbuildinfo .wrangler .shippie-dev-state"
  },
  "dependencies": {
    "hono": "^4.12.12",
    "@shippie/shared": "workspace:*",
    "@shippie/session-crypto": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "@types/bun": "^1.3.12",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
