// Trust Center (5B) — cross-app surface. Client-only render because
// the on-device ledger never traverses the network.
export const ssr = false;
export const csr = true;
export const prerender = false;
