/**
 * PostCSS config for Tailwind CSS v4.
 * v4 is processed via @tailwindcss/postcss instead of the legacy
 * `tailwindcss` plugin.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
