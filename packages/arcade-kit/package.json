{
  "name": "@shippie/arcade-kit",
  "version": "0.0.1",
  "description": "Shared daily/streak/share/seed contract for Shippie games. Extracted from the sudoku/stack/golazo vertical slices: deterministic daily seeds, versioned puzzle identity, streak roll, save/resume envelope, and a Web-Share helper. Offline-first, server-optional.",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "typescript": "^5.7.0"
  }
}
