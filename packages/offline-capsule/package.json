{
  "name": "@shippie/offline-capsule",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Offline capsule protocol helpers for Shippie marketplace and wrapper service workers.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "echo 'lint placeholder'",
    "build": "echo 'no build - types-only'",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
