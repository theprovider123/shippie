{
  "name": "@shippie/container-bridge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "bun test src",
    "build": "echo 'no build — container bridge source exported directly'",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@shippie/app-package-contract": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
