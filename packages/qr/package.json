{
  "name": "@shippie/qr",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Brand-styled QR codes for Shippie. One primitive, multiple consumers (invite share, marketplace share, device transfer, kiosk install, CLI deploy print).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "echo 'lint placeholder'",
    "build": "echo 'no build — types-only'",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "qr-code-styling": "^1.9.2"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.10.0",
    "@types/bun": "^1.3.12",
    "happy-dom": "15"
  }
}
