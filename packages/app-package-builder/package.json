{
  "name": "@shippie/app-package-builder",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./filesystem": {
      "types": "./src/filesystem.ts",
      "import": "./src/filesystem.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "bun test src",
    "build": "echo 'no build — package builder source exported directly'",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@shippie/app-package-contract": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
