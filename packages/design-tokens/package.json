{
  "name": "@shippie/design-tokens",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shippie's canonical design tokens. One source of truth for the brand — sunset/sage/marigold palette, Fraunces + General Sans + JetBrains Mono, sharp corners. Showcases import this and inherit the look.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./tokens.css": "./src/tokens.css",
    "./tailwind-theme.css": "./src/tailwind-theme.css",
    "./checklist.md": "./src/checklist.md"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "echo 'lint placeholder'",
    "build": "echo 'no build — types-only'",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.10.0",
    "@types/bun": "^1.3.12"
  }
}
