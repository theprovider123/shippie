{
  "name": "@shippie/share",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local-anonymous content sharing for Shippie. Pack a recipe / journal entry / glimpse into a signed blob, encode it in a URL fragment or QR, send via mesh / AirDrop / scan / text. No account required, no server holds the bytes.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "echo 'lint placeholder'",
    "build": "echo 'no build — types-only'",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@shippie/qr": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.10.0",
    "@types/bun": "^1.3.12",
    "happy-dom": "15"
  }
}
