{
  "name": "@shippie/spaces",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Private space, invite capsule, encrypted gossip, and local queue primitives for Shippie apps.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test src",
    "lint": "tsc --noEmit",
    "build": "echo 'no build — types-only'",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@shippie/proximity": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}

