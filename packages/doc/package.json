{
  "name": "@shippie/doc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Signed, encrypted, local-first Document primitives for Shippie apps. Hubs store sealed copies; users keep the keys.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "reference:read": "bun scripts/reference-reader.mjs",
    "lint": "echo 'lint placeholder'",
    "build": "echo 'no build — types-only'",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
